# bootstrap.xml 이 존재하면 application.xml 보다 우선순위가 높아 먼저 외부 config server 로 부터 프로퍼티 값을 가져온다
spring:
  cloud:
    config:
#      uri: http://127.0.0.1:8888  # config server uri
      uri: http://220.126.251.96:8888
      name: user-service         # 따로 저장한 YAML 파일명
#  profiles:
#    active: native